<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/zyqt_background" >

    <cmccsi.mhealth.portal.sports.view.XListView
        android:id="@+id/lv_campaignlist"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="5dp"
        android:divider="@color/zyqt_background"
        android:dividerHeight="8dp" >
    </cmccsi.mhealth.portal.sports.view.XListView>

    <TextView
        android:id="@+id/tv_emptyView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="30dp"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:text="@string/zyqt_text_campaign_empty"
        android:textSize="22sp"
        android:visibility="gone" />

</RelativeLayout>